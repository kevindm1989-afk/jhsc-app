# Pre-commit hook configuration
#
# Install: pip install pre-commit && pre-commit install
# Run on demand: pre-commit run --all-files
#
# These hooks catch obvious issues before they hit the repo.
# They are the cheapest gate — must be fast (< 5 seconds total).

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=500']
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: detect-private-key
      - id: mixed-line-ending
        args: ['--fix=lf']

  # Secrets detection
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.0
    hooks:
      - id: gitleaks

  # --- Language-specific (uncomment what applies) ---

  # Prettier (JS/TS/CSS/MD/JSON)
  # - repo: https://github.com/pre-commit/mirrors-prettier
  #   rev: v3.1.0
  #   hooks:
  #     - id: prettier

  # ESLint
  # - repo: local
  #   hooks:
  #     - id: eslint
  #       name: eslint
  #       entry: npx --no-install eslint --max-warnings=0
  #       language: system
  #       types: [javascript, jsx, ts, tsx]
  #       pass_filenames: true

  # Ruff (Python)
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.6.0
  #   hooks:
  #     - id: ruff
  #       args: [--fix]
  #     - id: ruff-format

  # gofmt
  # - repo: https://github.com/dnephin/pre-commit-golang
  #   rev: v0.5.1
  #   hooks:
  #     - id: go-fmt
  #     - id: go-vet

  # rustfmt
  # - repo: local
  #   hooks:
  #     - id: rustfmt
  #       name: rustfmt
  #       entry: cargo fmt --check
  #       language: system
  #       types: [rust]
  #       pass_filenames: false
